# LittleSlot 需求细化与交接记录

更新时间：2026-09-14

最新决策见第 17 节；与旧流程冲突时以第 17 节为准。仍处于规划阶段，尚未授权编写插件或后端代码。

## 1. 当前进度与工作边界

项目目标：实现 Bukkit / Paper 插件，限制单个 LittleSkin 账号可以进入 Minecraft 服务器的角色数量，角色以 UUID 判断。

原始需求见 [require.md](require.md)。当前仓库只有说明文件等基础文件，尚未编写插件代码，也没有进行真实 OAuth 授权联调或服务器兼容性测试。

用户当前要求是调研、细化需求、持续提问并提出加强建议。本文件用于换设备后的继续讨论。

**重要：原始需求明确禁止未经用户允许编写代码；须规划完善后再开始。用户尚未批准开始实现。写本交接文档不代表批准实现。**

状态标记：

- 已明确：来自用户原始需求。
- 文档已核对：来自官方文档，尚不等于实测成功。
- 建议：用于讨论的默认方案，未经用户确认。
- 待确认：不能擅自当成最终需求。

## 2. 已明确的需求

1. 按 LittleSkin 账号限制可入服角色数量。
2. 基于 Bukkit / Paper；以 26.2 为当前基准，不需大改或重构时尽量兼容旧版本。
3. 使用 UUID 判断角色，不能以用户名作为身份依据。
4. 配置广泛：至少包含最大角色数、是否入服即要求绑定。
5. 文本可配置：至少包含绑定提示、踢出原因等。
6. 管理员可以用一行指令查询、删除、修改相关数据。
7. 至少支持 SQLite 与 MySQL，默认 SQLite，可通过配置选择。
8. 先研究 LittleSkin OAuth 和角色接口，再与用户讨论、完善规划，获得实现授权后编写插件。

## 3. 第一轮问题：已回答 A / B / A

用户已确认：Q1 选 A（固定入服 UUID 名额，离线仍占用）；Q2 选 B（混合登录）；Q3 选 A（公网 HTTPS 回调、授权代码流）。以下保留选项用于解释决策。

用户进一步提出：先由混合登录插件鉴权（如有），玩家正式进服后再由 LittleSlot 判断 LittleSkin 绑定与名额。此流程可行性与边界见第 14 节；具体登录插件和正版玩家策略尚待确认。

### Q1：角色数量限制的含义

- A：限制获准入服的 UUID 名单大小，退出游戏后仍占名额，换角色需要释放名额。建议采用。
- B：只限制同账号同时在线的角色数量，离线后立即释放。
- C：LittleSkin 账号拥有的角色总数超过 N，则整个账号不能入服。

这三者的数据结构、并发处理和用户体验不同。例如账号拥有 A、B、C 三个角色、上限为 2：A 方案允许选其中两个；B 方案允许轮流使用三个但最多同时在线两个；C 方案直接拒绝。

### Q2：服务器登录方式

- A：仅 LittleSkin 外置登录。建议作为首期范围。
- B：LittleSkin 与正版混合登录，需要确认具体登录插件、代理和 UUID 映射方式。
- C：离线登录或尚未确定，需要另行定义可信身份来源。

插件读取到 UUID，不代表这个 UUID 已经由可信登录流程验证。OAuth 证明用户控制某个 LittleSkin 账号，游戏连接的身份验证还需服务端登录链路保证。

### Q3：OAuth 部署方式

- A：可以提供公网 HTTPS 回调，采用授权代码流。
- B：不希望自建回调，愿意申请 LittleSkin 设备代码流白名单。
- C：还未决定，需要结合实际部署条件比较。

已确认准备公开分发，不针对用户自己的特定服务器。OAuth 应用由每个服主各自创建，还是项目提供公共应用仍待确认。公共应用的可用性、额度与运维不能默认已有保障；不能在公开 JAR 中内置共享客户端密钥。

## 4. OAuth 与角色识别调研

### 4.1 身份模型

建议分别记录：

- LittleSkin 账号：`uid`，是名额归属对象。
- 游戏角色：Yggdrasil 档案的 `id`，解析为 UUID，是入服判断对象。
- 用户名：仅用于展示、搜索提示和最后已知名称，不作为身份主键。
- OAuth 授权：用于读取账号与角色归属，不等于名额本身。
- 入服名额：某账号下哪些 UUID 已获准进入指定范围的服务器。

普通角色接口返回的 `pid` 是角色记录编号，不能当作 Minecraft UUID。

### 4.2 文档已核对的接口

| 用途 | 请求 | 权限 / 结果 |
| --- | --- | --- |
| 获取授权账号身份 | `GET https://littleskin.cn/api/user` | `User.Read`；响应含 `uid` |
| 获取授权账号全部角色 UUID | `GET https://littleskin.cn/api/yggdrasil/sessionserver/session/minecraft/profile` | `Yggdrasil.PlayerProfiles.Read`；数组元素含 `id`、`name`、`properties` |
| 普通角色列表 | `GET https://littleskin.cn/api/players` | 本项目优先使用上面的 Yggdrasil 档案接口，避免依赖角色名换算 UUID |

请求需要 `Authorization: Bearer <access_token>` 和 `Accept: application/json`。不要解析访问令牌来提取账号身份，将其视为不透明字符串。

注意：`Yggdrasil.PlayerProfiles.Read` 与 `Yggdrasil.PlayerProfiles.Select` 冲突，不应同时申请。本项目读取所有角色，建议申请 Read。无需为了读取 UUID 申请创建 Minecraft 登录令牌或修改角色的权限。

建议基础权限为 `User.Read Yggdrasil.PlayerProfiles.Read`；是否需要持久刷新授权，取决于归属复核策略。

### 4.3 授权代码流

文档入口：`https://littleskin.cn/oauth/authorize`；兑换及刷新令牌：`https://littleskin.cn/oauth/token`。

部署需要客户端 ID、客户端密钥、注册的回调地址。建议插件内置仅承担回调的 HTTP 服务，由反向代理提供 HTTPS；是否采用独立服务待确认。

建议流程：创建与目标游戏 UUID 绑定的一次性会话 → 玩家打开授权链接 → 校验回调 state 与有效期 → 后端兑换令牌 → 获取 UID 与角色列表 → 校验归属 → 分配名额。

应防止回调重复提交、会话串用和重放。LittleSkin 授权代码流文档当前说明不支持 PKCE；state 回传等安全相关行为需要在正式实现前联调确认。

### 4.4 设备代码流

设备代码端点：`https://open.littleskin.cn/oauth/device_code`。

轮询及刷新端点：`https://open.littleskin.cn/oauth/token`。

需要申请应用白名单；初次通常处于仅创建者可授权的测试模式，正式投入使用需要申请解除。申请过程由用户完成，当前未申请、未发送邮件。

插件向玩家展示用户代码和验证链接；设备代码只保留在服务端。按响应的 interval 轮询，处理等待、减速、过期、拒绝和服务错误，限制重复发起与并发会话数量。

如需 refresh_token，需申请 `offline_access`。无需为了读取 UID 额外引入 ID Token 验证，可以继续调用用户信息接口。

### 4.5 错误与令牌处理

- 不能仅凭 HTTP 200 判定 API 成功：文档说明权限不足可能通过 JSON 内的 `code: 403` 表达。
- 令牌刷新后旧令牌失效，刷新与保存必须串行协调；跨服共享授权时也要避免并发刷新。
- 不在日志、命令结果或踢出消息输出访问令牌、刷新令牌、客户端密钥、设备代码。
- 如持久保存令牌，建议加密保存，密钥与数据库分离；具体配置与迁移方式待定。
- 网络请求和数据库操作不阻塞游戏主线程，设置超时、有限重试及限流。

### 4.6 调研限制

官方手册声明部分 API 仍处于试验阶段。当前只核对手册，没有用真实账号验证接口返回、权限申请、UUID 一致性及授权撤销行为。

手册链接的 OpenAPI YAML 本次未成功读取，不能声称已核对 OpenAPI 契约。实现前应读取 OpenAPI 并联调；手册说明与 OpenAPI 不一致时以 OpenAPI 为准。

## 5. 建议的核心流程（以 Q1 选 A 为前提）

1. 可信登录流程提供当前游戏 UUID。
2. 查询其名额、禁用状态与归属验证有效期。
3. 已获准且验证有效的角色按规则入服。
4. 未绑定或需要重新授权的角色进入绑定流程。
5. OAuth 完成后获取 LittleSkin UID 和全部角色 UUID。
6. 当前游戏 UUID 不在列表中时拒绝绑定，并说明登录角色与授权账号不匹配。
7. 在数据库事务中检查禁用状态、账号名额和已有记录；同一 UUID 重复请求保持幂等。
8. 名额可用时添加当前角色；已满则提示账号已用名额及申请管理员处理的方式。
9. 在有效的游戏会话中解除限制，或提示玩家重新连接；具体体验由绑定模式决定。

获取全部角色列表不自动把全部角色占入名额。第 16 节已更新需求：一次授权后保存账号的角色归属索引，后续其他已知角色无需逐个授权，但仍需原子检查并分配名额；验证新鲜度策略待定。

## 6. 下一轮必须细化的业务规则

| 主题 | 待确认问题 | 建议起点 |
| --- | --- | --- |
| 默认上限 | 默认 N 是多少？是否允许账号独立上限？0 表示禁用还是无限？ | 全局默认 + 账号覆盖；无限值应明确约定 |
| 强制绑定 | 在入服前拒绝并给链接，还是进入后限时绑定？ | 两种模式可配置，但首期主流程需要选定 |
| 受限入服 | 未绑定时是否禁止移动、交互、指令、聊天、拾取和伤害？多久超时？ | 如选择入服后绑定，应给明确指令白名单和超时行为 |
| 可选绑定 | 不强制时游客能否正常游玩？ | 明示这种模式不能完整限制未知账号的小号 |
| 名额释放 | 仅管理员，还是玩家也能释放自己的名额？是否冷却？ | 初期管理员处理，避免反复换号规避限制 |
| 删除含义 | 删除记录后能否重新绑定？ | 释放名额、解除授权、禁止角色分别定义 |
| 上限调低 | 已有 3 个名额改为 2 时保留谁？是否踢在线玩家？ | 不任意删记录；明确超额处理方案 |
| 角色变化 | 改名、删除重建、转移账号后如何处理？ | 按 UUID 与新归属复核，不按同名恢复权限 |
| 归属复核 | 每次登录、定时、缓存到期还是只绑定时检查？ | 缓存 + 到期复核；周期待定 |
| 授权撤销 | 是否立即拒绝、给予宽限或要求重新绑定？ | 区分明确失效与临时网络故障 |
| 外部账号状态 | 是否因 LittleSkin 封禁状态拒绝入服？ | 单独配置，不能默认当成本服封禁 |
| 故障 | LittleSkin 不可用、数据库不可用分别怎么办？ | 新绑定拒绝；已验证角色可配置有限缓存宽限 |
| 例外 | 管理员、OP、特定 UUID 是否绕过？ | 显式权限或例外记录，并说明是否占名额 |
| 服务器范围 | 单服独立，还是群组服共享 N？ | 单服默认；共享使用显式 scope 标识 |
| 在线变更 | 管理员禁用或释放在线角色后立即踢出吗？ | 禁用立即生效；其他动作单独规定 |
| 数据保留 | 历史角色、审计、失效授权保留多久？ | 设清理策略，但清理不能意外重置限制 |

## 7. 命令与权限草案

以下只是待审阅的命令设计，不是已实现接口。

| 命令 | 预期用途 |
| --- | --- |
| `/littleslot bind` | 发起自己的绑定或重新授权 |
| `/littleslot status` | 查看自己绑定、名额与验证状态 |
| `/littleslot account <uid>` | 管理员查看账号上限及角色名单 |
| `/littleslot player <uuid>` | 管理员查看角色归属、名额和禁用状态 |
| `/littleslot release <uuid>` | 释放入服名额；不代表禁止再次申请 |
| `/littleslot limit <uid> <数量>` | 修改账号独立上限 |
| `/littleslot limit <uid> reset` | 恢复全局上限 |
| `/littleslot block <uuid>` | 禁止角色入服 / 再申请，是否同时释放名额待定 |
| `/littleslot unblock <uuid>` | 解除禁止，不自动授予名额 |
| `/littleslot reload` | 校验并重载允许热更新的配置 |
| `/littleslot doctor` | 检查配置、数据库、OAuth 设置与登录环境 |

建议控制台可执行所有管理命令；每类管理操作独立权限节点。用户名可用于搜索，但最终变更应定位到确定 UUID，重名或历史名歧义不可猜测。

管理员强制修改归属会绕过 OAuth 证明，需要确认是否提供；如提供，应与已验证绑定区分并记录审计。删除、修改均应给出明确结果与影响，不依赖多步菜单完成常规操作。

## 8. 配置草案

建议按以下类别组织，具体键名与默认值在规则确认后再定：

- 名额：默认上限、账号覆盖、作用范围、例外策略。
- 绑定：强制模式、等待时间、提醒间隔、会话有效期、允许命令、重复请求冷却。
- OAuth：流程、客户端 ID、密钥来源、回调地址、监听地址与端口、连接和读取超时。
- 复核：缓存有效期、刷新周期、授权失效处理、外部故障宽限。
- 数据库：SQLite 文件路径；MySQL 地址、库名、用户名、密码来源、TLS、连接池与超时。
- 消息：绑定提示、链接、授权成功 / 失败、归属不匹配、名额已满、禁用、超时、服务不可用、管理结果。
- 展示：中文默认、是否需要英文、颜色格式、链接点击能力、旧版本降级展示。
- 运维：日志级别、审计保留、请求限流和诊断信息。

消息占位符建议包含角色 UUID、显示名、已用数、上限、授权链接、剩余秒数；展示账号 UID 等信息的范围需要限定。配置重载先验证再替换，失败保留旧配置。数据库切换与监听端口变化是否要求重启，应明确提示。

## 9. 数据模型与一致性草案

逻辑实体可包含：

- 账号：UID、独立上限、管理状态、创建 / 更新时间。
- 角色：UUID、当前已验证 UID、最后已知名字、归属验证时间。
- 名额：scope、UUID、所属 UID、分配时间与来源。
- 授权：UID、加密令牌、过期时间、版本 / 刷新协调信息。
- 禁止记录：目标类型、目标标识、原因、操作者与时间。
- 审计：动作、目标、变更前后、操作者、时间。
- 数据库版本：支持后续结构升级。

待 Q1、跨服范围和禁用语义确认后再确定表结构。绑定会话可优先放内存，重启失效；如需跨实例回调则需共享或路由设计。

必须保证：同一 scope 内角色名额唯一；检查剩余名额与写入在同一原子操作中完成；失败不会留下半条绑定；跨服不能只依赖 JVM 内锁。

SQLite 和 MySQL 通过存储层封装差异，分别验证事务行为。切换数据库连接不等于迁移数据。是否首期提供迁移命令待确认；迁移应包含停写或维护窗口、备份、计数与关键约束校验、失败恢复路径。

## 10. 版本与部署范围

文档已核对：Paper 26.1+ 使用 Java 25；`plugin.yml` 的 api-version 高于服务端时会拒绝加载。因此以 26.2 验收，不代表应将最低加载版本也设为 26.2。

建议使用稳定 Bukkit API 和传统 plugin.yml，避免 NMS、CraftBukkit 内部类及不必要的 Paper 专属依赖。插件字节码、第三方库最低 Java 要求和服务器运行 Java 分开规划。

仍需用户明确：

1. 最低必须支持 1.8、1.12、1.16，还是只尽量兼容？
2. 是否必须同时验收 Spigot 与 Paper？
3. 是否有 Velocity / BungeeCord、具体身份转发设置？
4. 是否包括 Folia、Geyser/Floodgate、混合登录插件？这些不自动包含在普通 Bukkit/Paper 兼容承诺中。
5. 单服预计在线量、账号量和绑定高峰规模？

应以“已测试版本”和“预计兼容版本”区分发布说明，不承诺未经测试的全版本兼容。

## 11. 验收场景草案

后续获得实现授权后，至少验证：

1. 正常 OAuth 绑定，正确保存 UID 与 UUID；错误账号不能绑定当前角色。
2. N 个角色成功，第 N+1 个被拒绝；同 UUID 重试不重复占用。
3. 两个角色争抢最后一个名额只有一个成功；共享数据库时跨实例同样成立。
4. 改名不依赖用户名重新识别；删除重建、归属转移按已确认规则处理。
5. 授权拒绝、过期、重复回调、state 错误和轮询限流处理正确。
6. 令牌刷新不丢失新令牌；权限不足的 HTTP 200 不被当作成功。
7. 接口超时与数据库故障不阻塞主线程、不意外放行未知角色。
8. release、block、limit 的行为与在线生效策略一致，审计可查询。
9. SQLite / MySQL 数据重启后保持；如提供迁移，迁移前后关键数据一致。
10. 配置错误重载保留原配置；自定义文本及占位符工作正常。
11. 26.2 与选定旧版本实际启动、登录、绑定、踢出和命令可用。

## 12. 官方参考资料

- LittleSkin 手册：https://manual.littlesk.in/
- OAuth 总览：https://manual.littlesk.in/advanced/oauth2/
- 授权代码流：https://manual.littlesk.in/advanced/oauth2/authorization-code-grant
- 设备代码流：https://manual.littlesk.in/advanced/oauth2/device-authorization-grant
- LittleSkin API：https://manual.littlesk.in/advanced/api
- OpenAPI（本次未成功读取）：https://manual.littlesk.in/littleskin-api.openapi3_1.yaml
- Blessing Skin 用户信息：https://blessing.netlify.app/api/user
- Blessing Skin 角色接口：https://blessing.netlify.app/api/players
- 服务端外置登录：https://manual.littlesk.in/yggdrasil/server
- Paper Java 要求：https://docs.papermc.io/paper/getting-started/
- Paper plugin.yml：https://docs.papermc.io/paper/dev/plugin-yml/

## 13. 换设备后的继续方式

可直接发送：

> 请阅读 require.md 和 PLANNING.md，继续细化 LittleSlot 需求。先与我确认第 3 节的三个问题，再分批讨论第 6 节规则与部署范围。区分已确认需求、建议和待实测事项；在我明确同意开始实现前不要编写插件代码。

下一步产出应是确认后的实现规格：名额语义、完整玩家流程、异常行为、命令权限、配置默认值、数据模型、部署说明和测试矩阵。当前不要将本文建议直接视作已批准的产品决策。

## 14. 第一轮确认后的继续讨论

已确认 A / B / A，尚未授权编写插件代码。第 13 节的历史续接提示已完成，应从本节继续。

用户提出的“先鉴权、进服后检查”可以作为流程设计，但必须以实际登录插件的鉴权完成信号为准，不能仅因收到进服事件或等待固定秒数就视为鉴权成功。没有混合登录插件时，也需由可信服务端登录链路提供身份。具体插件、代理和 UUID 配置待确认及联调。

建议流程：可信鉴权完成 → 玩家入服后处于待检查状态 → 异步检查已有绑定或引导 OAuth → 校验角色归属并原子分配名额 → 放行游玩；拒绝或超时则踢出。检查前限制交互的具体范围尚待用户确认。若在检查前允许自由游玩，实际语义为进服后移除不合格玩家，不能保证其完全没有游戏行为。

混合登录需要区分服务端实际 UUID 与 LittleSkin Yggdrasil UUID；不得按同名认定归属。若登录插件改写 UUID，需要可信映射或针对该插件适配。

正版玩家策略已确认：仅 LittleSkin 登录角色受限，经过正版鉴权的玩家直接放行，不要求绑定或占名额。必须识别本次登录的可信身份来源，不能仅凭名字、UUID 外形或账号拥有正版认定本次为正版登录。

本次核对官方 API 文档：`GET /api/premium-verification` 在 `PremiumVerification.Read` 权限下返回正版验证状态，存在时含正版 UUID。它可作为正版归属验证的候选路径，需要用户确认是否要求先在 LittleSkin 完成正版验证，并在实现前核对 OpenAPI 和实测；尚未批准采用或新增该权限。来源：https://manual.littlesk.in/advanced/api

上述正版关联候选路径因已确认正版豁免，不纳入当前绑定方案；正版验证状态不能证明本次游戏连接使用了正版鉴权。

## 15. 公开发布与第二轮确认

用户已确认：

- 插件面向公开发布，需要广泛适配，不围绕用户自己的某个登录插件定制。
- 正版登录直接放行，不受名额限制；只针对 LittleSkin 登录。
- 接受检查与绑定期间暂时限制移动、交互等操作，通过后恢复。
- 继续讨论业务问题与增强建议；尚未授权开始编写插件代码。

身份适配建议（尚未确认为实现规格）：

- 将名额核心与登录来源适配层分开，提供扩展接口；按能力检测并接入受支持的登录插件或代理。
- 区分待鉴权、已验证正版、已验证 LittleSkin、其他来源、无法识别，不把无法识别等同于正版。已验证其他来源不属于当前 LittleSkin 限制范围；如何安全识别和放行需纳入适配契约。
- 纯 LittleSkin 部署可由服主明确声明模式，但声明不能代替实际登录链路鉴权。混合部署需要可信来源信号；缺少适配时给出诊断，不能宣传自动支持任意插件。
- 未知身份策略建议可配置：拒绝并说明配置问题（建议默认），或显式放行并提示该部署不能完整执行限制。
- UUID 改写环境应同时保留可信来源的原始角色 UUID 与游戏会话 UUID；无可信映射时不以用户名猜测。

下一轮优先确认：每个服主独立创建 OAuth 应用还是公共服务；无法识别登录来源时的策略；默认名额上限与玩家自助释放策略。后续再确认超额、授权复核、故障宽限和群组服范围。

## 16. 2026-09-14 最新需求与待定实现细节

### 16.1 用户明确的调整

- 进服并排除正版后，查询数据库中的 UUID；未知 UUID 提示 LittleSkin 绑定；已知 UUID（含之前已授权账号的其他角色）自动处理入服并计算数据。
- OAuth 需按需兼容“公共授权后端 + 自建后端 + 私有代码流”。
- 公共后端采用 API Key 与调用付费机制；发卡接口暂定爱发电 + 订单号。
- 先说明公共后端调用逻辑，由用户决定保存哪些数据；本次不替用户确定持久化范围。
- 要求比较常见登录插件普及程度与适配难度，再总结待确认问题。

### 16.2 自动识别流程的设计解释

为保持原来的固定名额上限，建议将“免重复 OAuth”与“允许入服”分开：UUID 命中归属索引后，先处理名额、禁用和归属有效性，再放行，而非无条件先放行。

需要两个不同概念：

- 角色归属索引：LittleSkin UID → 该次授权读取到的全部角色 UUID，含验证时间及来源；未进服的角色也可存在。
- 入服名额：scope + UUID → UID，只有该角色实际通过名额检查时才新增；重复进服不重复计数。

例：账号有 A/B/C、上限 2。A 首次授权后同步 A/B/C 归属，仅 A 占名额；B 入服免授权并占第 2 个名额；C 虽已知归属仍因满额拒绝。此例是对新需求与既有上限的协调解释，等待用户确认。后续新建 D 在未同步前仍属于未知 UUID，按用户流程发起绑定。

查询默认建议使用本服/明确共享 scope 的索引；不得因另一无关服务器曾授权同一账号就默认共享授权或披露完整角色名单。角色转移、删除、授权撤销及缓存过期的处理尚待确认。

### 16.3 三种 OAuth 模式的暂定解释

1. 公共模式：插件调用项目公共后端，使用 API Key 和额度；公共 OAuth 密钥只存在公共后端。
2. 自建模式：服主部署同协议后端，使用自己的 OAuth 应用；无需依赖公共付费服务。
3. 私有代码流：暂按“插件直接处理授权代码流，服主提供自己的应用和 HTTPS 回调”理解；用户是否实际指设备代码流需要澄清。设备代码流不是授权代码流的别名，也尚未明确加入本轮三种模式。

### 16.4 公共后端调用草案（接口名非已实现承诺）

1. 服主通过付款及订单领取流程获得 API Key，并配置公共后端地址。
2. 本地角色索引和名额检查通过时，不需要请求公共后端；公共服务不按此类本地进服扣费。
3. 未知 UUID 由插件创建授权会话，请求携带 API Key、实例标识、目标原始角色 UUID、请求幂等标识。后端校验 Key、额度与限流，返回会话 ID、授权链接、有效期及查询间隔；实例标识不能代替鉴权。
4. 浏览器在 LittleSkin 授权后回到公共 HTTPS 后端；后端校验一次性 state、会话归属和期限，兑换令牌，读取 UID 及全部角色 UUID，核对目标角色归属。
5. 插件带 Key 查询本实例会话结果；返回 UID、角色 UUID 列表、验证时间等所需字段，不默认下发 OAuth 原始令牌。跨 Key/实例查询必须被拒绝；Key 不出现在玩家链接里。
6. 本服事务同步归属索引，检查名额和禁用，分配当前角色名额后恢复玩家操作。回调返回时仍需验证游戏会话未过期或被替换。
7. 若未来支持自动复核，由实际持有有效刷新令牌的一方执行，复核是否另收费、结果缓存与调用去重规则待定。

计费建议：按一次成功的归属验证业务操作计 1 次，而非每次 HTTP 轮询计费；创建时原子预占额度，验证成功时结算，取消/超时/错误释放预占，回调和客户端重试幂等。成功验证后本服因名额已满拒绝仍属于已完成云端验证，是否计费需用户明确。上游失败、UUID 不匹配不计费是建议，不是用户已确认规则；需另有限流防止滥用。每次领取结果、结果确认不重复扣费，查询也仍受速率限制。

### 16.5 保存数据的待选边界

- 公共付费服务基础持久状态：客户/实例、API Key 摘要与状态、订单兑付归属、额度及扣费流水、幂等结果；这些支撑重启后额度、去重及账务一致性。具体字段与保留期待定。
- 授权会话短期状态：目标 UUID、会话所属客户/实例、state 校验值、过期时间、操作状态及结果；可短期内存或 TTL 存储，是否要求重启恢复待定。
- 可选长期状态：LittleSkin UID 与全角色列表、刷新令牌、授权版本与复核时间。长期保存支持自动复核；不保存也不交付令牌给其他组件时，令牌丢弃后后端无法自行拉取新列表，需重新授权。
- 插件数据库：归属索引与入服名额必须分别记录才能实现已知其他角色自动识别；数据范围、验证期限和清理策略待确认。
- 不建议保存：LittleSkin 密码（流程不会获取）、无关个人信息、完整原始 HTTP 请求/响应、明文日志中的 Key 和令牌。

### 16.6 爱发电接入草案

官方文档支持按 out_trade_no 查询订单，也提供 Webhook；文档提示通知可能延迟或重复，因此建议通知触发服务端订单查询并幂等入账。核对实际交易状态、商品/方案、实付金额及兑付记录后发 Key 或充值。平台 API Token 仅保存在支付后端。

订单号是查询标识，不自动证明领取者是购买人，也不应直接作为长期 API Key。需要购买人关联或一次性领取凭证；可评估下单备注预绑定随机领取码。具体发卡渠道、退款/撤单处理和当前爱发电接口能力需联调确认。来源：https://guide.afdian.com/creator/developer

### 16.7 登录插件调研边界

没有得到可横向比较的实际使用人数。公开 Spigot 累计下载量只作同平台历史传播参考，GitHub 星标另列，不混成用户人数排名。适配难度是基于职责和部署结构的初评，未经适配实测。

- LoginSecurity：Spigot 页面约 318.5 万下载；密码登录，不能单独证明 LittleSkin 来源；鉴权时序适配预计低至中。
- AuthMeReloaded：约 160.5 万下载；密码登录，需另接登录来源提供方；鉴权时序预计低。
- FastLogin：约 32.2 万下载；正版自动鉴权，常与 AuthMe 搭配；正版会话与 UUID 策略适配预计中，不单独提供 LittleSkin 来源识别。
- MultiLogin（CaaMoe）：约 482 星标；正版与多个外置 Yggdrasil 共存，需求相关性高；代理身份转发和 UUID 映射预计中高，官方仓库声明停止维护。
- LimboAuth：约 238 星标；Velocity 登录体系，代理到 Bukkit 的可信桥接预计中高；不能默认代表多 Yggdrasil 支持。
- nLogin：有官方 API 和多平台支持，实际人数本次未取得；正版与会话接口及不同部署方式预计中；不能将 OpeNLogin 的历史下载量冒充 nLogin 当前用户数。

资料：https://www.spigotmc.org/resources/loginsecurity.19362/ 、https://www.spigotmc.org/resources/authmereloaded.6269/ 、https://www.spigotmc.org/resources/fastlogin.14153/ 、https://github.com/CaaMoe/MultiLogin 、https://github.com/Elytrium/LimboAuth 、https://docs.nickuc.com/ 、https://jd.nickuc.com/nlogin/com/nickuc/login/api/nLoginAPI.html

另可提供纯 LittleSkin/authlib-injector 部署模式，但后者是 Java agent，不是普通 Bukkit 登录插件；须明确服务端真实采用单一可信来源，不能用配置声明掩盖混合来源。

## 17. 三种模式与公共后端职责最终澄清

用户明确三种授权模式为：

1. 公共授权后端。
2. 服主自行配置授权代码流。
3. 服主自行配置设备代码流。

第 16.3 节关于“自行部署同协议后端”的解释被本节取代，不作为单独要求支持的第四种模式。设备代码流需要服主为相应应用申请白名单及生产授权；配置与实际接口联调尚未完成。

用户明确公共后端只负责完成服务器插件提交的归属验证申请，成功后计费并返回结果，不提供其他业务功能。名额、绑定关系长期存储、角色自动同步、定时归属复核等不属于公共后端职责。公共后端按需处理请求，不主动为插件执行后台任务。

API Key、额度扣减和最小账务/幂等状态是完成已要求计费的支撑机制，不扩展为管理平台或账号托管业务。之前讨论的爱发电与订单号为暂定发卡渠道；支付接入是否独立于验证服务、如何补充额度尚待澄清，不擅自实现订单管理系统。

待交互确认：成功是指完成目标 UUID 归属证明还是任意成功返回查询结果；如何处理不匹配及本服名额不足；授权结果短期保留与重试去重期限、处理结束后令牌删除方式；本地索引验证有效期；默认上限及名额释放规则；首期身份适配列表。长期保存玩家授权未获批准。

### 17.1 交互已确认的规则

- 计费成功：成功取得账号角色列表即算成功；目标角色不匹配仍计费，玩家取消和接口故障不计费。获取 UID 与列表、API 业务错误判定和扣费原子性仍需在接口规格中落实，不能把 HTTP 200 一律视为成功。
- 完成数据读取后删除 OAuth 令牌，授权结果保留 10 分钟供重试领取；最小计费与去重记录持久保存。不长期保存刷新令牌。
- 插件本地归属默认 30 天有效，可配置；过期后再入服需重新授权验证，公共模式成功调用重新计费。名额记录与归属有效期分离，过期不能自动释放名额，重新验证不能重复占名额。
- 同一请求重复回调、轮询及结果领取不重复扣费，明确计费失败时不返回可消费的成功结果；网络丢包不撤销已成功落账的验证，可在保留期内重新领取。

### 17.2 第二批交互已确认

- 默认每个 LittleSkin 账号 2 个名额，支持账号独立上限。
- 玩家是否允许自助释放、冷却时间均可配置；默认允许释放、7 天冷却。提问选项以每账号冷却为起点，但计时起点、跨服范围、释放当前在线角色及管理员是否绕过冷却仍需细化。
- 无法识别登录来源的策略可配置，默认放行并记录。此模式下无法识别的 LittleSkin 登录可能绕过限制，不能宣传为对全部未知登录强制限制。

本轮交互不代表授权开始编码。下一批优先细化：首期适配范围；名额释放时在线生效方式和冷却计时；账号上限降低后的处理；额度不足、公共服务故障及归属到期后的玩家体验；发卡归属与单 Key 多服策略。

## 18. 最小通信方案与集中问题表（等待回答）

用户要求尽量减少插件向公共后端上传的数据，提出仅提交插件鉴权，获取验证地址和查询地址，由插件轮询取得账号角色列表。建议请求仅含 API Key 与不含玩家身份的随机幂等编号；目标 UUID、玩家名及游戏会话留在本地。后端不再核对目标角色归属，由插件自行核对。成功读取账号数据按第 17 节规则计费。

建议成功响应包含 LittleSkin UID 与 UUID 列表（不含姓名、邮箱、皮肤属性），另有等待/失败/过期状态。UID 用于角色集合变化后保持账号名额身份稳定；只返回 UUID 列表无法可靠关联完全更换角色的同一账号。此最小方案已说明，等待用户确认是否保留 UID。查询结果须校验创建者 Key 的权限，不能把知道查询地址视为唯一身份依据。API Key 只标识插件付费调用方，不证明游戏连接身份。

已按用户要求一次性提出集中交互问题，尚未收到本批回答，不将选项中的建议当作已批准决策。覆盖：最小协议、首期登录适配、最低版本、共享scope、释放冷却、在线释放、降低上限、绑定默认体验、归属到期、故障/欠费、旧归属变化、封禁、Key/额度范围、订单领取证明、定价与退款、去重保留、自配模式令牌策略、数据库迁移和展示/例外默认值。

既有待核验事项仍保留：LittleSkin 公共商业应用用途与调用额度、OAuth实际契约和授权状态行为、爱发电接入能力及售卖规则。以上外部事项不能通过勾选功能偏好代替核实；本次问题表不请求开始编码授权。

### 18.1 问题表恢复后的第一批确认

- 接受最小通信协议：API Key + 随机幂等编号请求，返回验证地址和查询地址，结果仅含必要状态及 UID + 角色 UUID 列表。
- 三种 OAuth 模式令牌策略一致：读取结果后删除令牌，不长期保存刷新令牌；归属默认 30 天到期重新授权。
- 支持共享 MySQL + 相同 scope 的群组服共享名额，默认单服独立；API Key 不决定名额和玩家数据共享范围。

原异步长表未能在用户界面保留，已改为每批最多三题的同步交互，以下各批答案单独记录；未回答的建议仍待定。

### 18.2 名额操作确认

- 自助释放冷却按 UID + scope 计算，从成功释放开始计 7 天，首次可立即释放；管理员操作不受冷却，也不重置玩家冷却。
- 释放当前在线角色立即踢出；它以后仍可按正常名额规则重新申请。释放不是封禁。
- 上限降低后保留已有名额和入服能力，禁止新增，随释放逐步回到新上限。

### 18.3 绑定、故障与归属变化确认

- 默认强制绑定，等待 5 分钟、每 30 秒提示；期间限制移动/交互/聊天，允许绑定、状态、释放、帮助等白名单命令；均可配置。
- 用户选择后端无法验证时临时放行，可配置，取代此前建议的有限宽限默认。有效本地记录继续按本地规则使用；临时放行不伪造已验证归属或名额记录。明确归属验证失败、封禁和名额已满不属于“无法验证”。故障恢复后复查时机待细化。
- 重新验证发现旧 UUID 已不属于账号时撤销其旧名额并踢出；归属索引同步失效。管理员封禁则立即踢出并保留名额；解除封禁不恢复已释放的名额。

### 18.4 Key 与订单确认

- 每个 API Key 独立额度，可以多服共用；支持撤销和更换。共用 Key 视为同一信任边界，不另建默认客户共享额度池。
- 订单采用“订单号 + 预生成领取凭证”，下单备注关联识别码，后端查单且每单只入账一次；需确认爱发电具体能力。
- 用户暂不确定价格、有效期、试用与退款细节；这些保持待定，不套用推荐默认值。

### 18.5 适配与运维范围确认

- 首期适配目标：纯 LittleSkin、AuthMe/FastLogin、MultiLogin，包含必要的代理桥接；具体版本及会话可信来源接口待核对。MultiLogin 已停止维护，按存量版本兼容目标处理，不承诺修复其自身缺陷。
- Minecraft 最低必须验收版本为 1.16.5，原需求新版本仍为验收目标；更旧版本仅尽力兼容。
- 首期包含 SQLite 与 MySQL 双向自动迁移，需停写/维护窗口、备份、约束和计数校验及失败恢复；共享数据库迁移需协调所有写入实例，不能只停本服。

### 18.6 最后一批默认值确认

- 结果默认保留 10 分钟，过期后相同请求编号只返回已过期，不重新扣费；明确重新授权使用新编号，成功计新一次。计费与去重记录默认保留 180 天，期限可配置；编号关联创建时间，超出去重窗口的旧请求必须拒绝，不能作为新任务执行。
- 故障恢复后分批重新检查临时放行的在线玩家，需要授权者进入受限状态并获得 5 分钟绑定期；恢复检查本身不自动创建付费授权任务。
- 默认中文消息，上限 0 禁止新增名额、-1 无限；不自动给予 OP 名额绕过。
- 首期不包含 Folia 与基岩版专门适配。
- 归属到期时，在该账号下一次有角色进服时要求重新授权；成功更新该 scope 内账号完整角色列表与默认 30 天有效期，不自动释放原名额。

集中问题表已通过同步分批全部收回答复；第 18 节前段的“等待回答”仅为历史记录。用户明确保留收费细节待定。发布前仍需完成官方商业用途/额度确认、爱发电能力验证、OAuth 与适配实测。未获得开始编码授权。
