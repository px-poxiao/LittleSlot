# LittleSlot 需求细化与交接记录

更新时间：2026-09-13

## 1. 当前进度与工作边界

项目目标：实现 Bukkit / Paper 插件，限制单个 LittleSkin 账号可以进入 Minecraft 服务器的角色数量，角色以 UUID 判断。

原始需求见 [require.md](require.md)。当前仓库只有说明文件等基础文件，尚未编写插件代码，也没有进行真实 OAuth 授权联调或服务器兼容性测试。

用户当前要求是调研、细化需求、持续提问并提出加强建议。本文件用于换设备后的继续讨论。

**重要：原始需求明确禁止未经用户允许编写代码；须规划完善后再开始。用户尚未批准开始实现。写本交接文档不代表批准实现。**

状态标记：

- 已明确：来自用户原始需求。
- 文档已核对：来自官方文档，尚不等于实测成功。
- 建议：用于讨论的默认方案，未经用户确认。
- 待确认：不能擅自当成最终需求。

## 2. 已明确的需求

1. 按 LittleSkin 账号限制可入服角色数量。
2. 基于 Bukkit / Paper；以 26.2 为当前基准，不需大改或重构时尽量兼容旧版本。
3. 使用 UUID 判断角色，不能以用户名作为身份依据。
4. 配置广泛：至少包含最大角色数、是否入服即要求绑定。
5. 文本可配置：至少包含绑定提示、踢出原因等。
6. 管理员可以用一行指令查询、删除、修改相关数据。
7. 至少支持 SQLite 与 MySQL，默认 SQLite，可通过配置选择。
8. 先研究 LittleSkin OAuth 和角色接口，再与用户讨论、完善规划，获得实现授权后编写插件。

## 3. 第一轮问题：已提出，尚未收到回答

换设备后优先继续这三个问题，不要假设已选择推荐项。

### Q1：角色数量限制的含义

- A：限制获准入服的 UUID 名单大小，退出游戏后仍占名额，换角色需要释放名额。建议采用。
- B：只限制同账号同时在线的角色数量，离线后立即释放。
- C：LittleSkin 账号拥有的角色总数超过 N，则整个账号不能入服。

这三者的数据结构、并发处理和用户体验不同。例如账号拥有 A、B、C 三个角色、上限为 2：A 方案允许选其中两个；B 方案允许轮流使用三个但最多同时在线两个；C 方案直接拒绝。

### Q2：服务器登录方式

- A：仅 LittleSkin 外置登录。建议作为首期范围。
- B：LittleSkin 与正版混合登录，需要确认具体登录插件、代理和 UUID 映射方式。
- C：离线登录或尚未确定，需要另行定义可信身份来源。

插件读取到 UUID，不代表这个 UUID 已经由可信登录流程验证。OAuth 证明用户控制某个 LittleSkin 账号，游戏连接的身份验证还需服务端登录链路保证。

### Q3：OAuth 部署方式

- A：可以提供公网 HTTPS 回调，采用授权代码流。
- B：不希望自建回调，愿意申请 LittleSkin 设备代码流白名单。
- C：还未决定，需要结合实际部署条件比较。

还需追问：这是仅供自己的服务器使用，还是准备公开分发？OAuth 应用由每个服主各自创建，还是项目提供公共应用？公共应用的可用性、额度与运维不能默认已有保障；不能在公开 JAR 中内置共享客户端密钥。

## 4. OAuth 与角色识别调研

### 4.1 身份模型

建议分别记录：

- LittleSkin 账号：`uid`，是名额归属对象。
- 游戏角色：Yggdrasil 档案的 `id`，解析为 UUID，是入服判断对象。
- 用户名：仅用于展示、搜索提示和最后已知名称，不作为身份主键。
- OAuth 授权：用于读取账号与角色归属，不等于名额本身。
- 入服名额：某账号下哪些 UUID 已获准进入指定范围的服务器。

普通角色接口返回的 `pid` 是角色记录编号，不能当作 Minecraft UUID。

### 4.2 文档已核对的接口

| 用途 | 请求 | 权限 / 结果 |
| --- | --- | --- |
| 获取授权账号身份 | `GET https://littleskin.cn/api/user` | `User.Read`；响应含 `uid` |
| 获取授权账号全部角色 UUID | `GET https://littleskin.cn/api/yggdrasil/sessionserver/session/minecraft/profile` | `Yggdrasil.PlayerProfiles.Read`；数组元素含 `id`、`name`、`properties` |
| 普通角色列表 | `GET https://littleskin.cn/api/players` | 本项目优先使用上面的 Yggdrasil 档案接口，避免依赖角色名换算 UUID |

请求需要 `Authorization: Bearer <access_token>` 和 `Accept: application/json`。不要解析访问令牌来提取账号身份，将其视为不透明字符串。

注意：`Yggdrasil.PlayerProfiles.Read` 与 `Yggdrasil.PlayerProfiles.Select` 冲突，不应同时申请。本项目读取所有角色，建议申请 Read。无需为了读取 UUID 申请创建 Minecraft 登录令牌或修改角色的权限。

建议基础权限为 `User.Read Yggdrasil.PlayerProfiles.Read`；是否需要持久刷新授权，取决于归属复核策略。

### 4.3 授权代码流

文档入口：`https://littleskin.cn/oauth/authorize`；兑换及刷新令牌：`https://littleskin.cn/oauth/token`。

部署需要客户端 ID、客户端密钥、注册的回调地址。建议插件内置仅承担回调的 HTTP 服务，由反向代理提供 HTTPS；是否采用独立服务待确认。

建议流程：创建与目标游戏 UUID 绑定的一次性会话 → 玩家打开授权链接 → 校验回调 state 与有效期 → 后端兑换令牌 → 获取 UID 与角色列表 → 校验归属 → 分配名额。

应防止回调重复提交、会话串用和重放。LittleSkin 授权代码流文档当前说明不支持 PKCE；state 回传等安全相关行为需要在正式实现前联调确认。

### 4.4 设备代码流

设备代码端点：`https://open.littleskin.cn/oauth/device_code`。

轮询及刷新端点：`https://open.littleskin.cn/oauth/token`。

需要申请应用白名单；初次通常处于仅创建者可授权的测试模式，正式投入使用需要申请解除。申请过程由用户完成，当前未申请、未发送邮件。

插件向玩家展示用户代码和验证链接；设备代码只保留在服务端。按响应的 interval 轮询，处理等待、减速、过期、拒绝和服务错误，限制重复发起与并发会话数量。

如需 refresh_token，需申请 `offline_access`。无需为了读取 UID 额外引入 ID Token 验证，可以继续调用用户信息接口。

### 4.5 错误与令牌处理

- 不能仅凭 HTTP 200 判定 API 成功：文档说明权限不足可能通过 JSON 内的 `code: 403` 表达。
- 令牌刷新后旧令牌失效，刷新与保存必须串行协调；跨服共享授权时也要避免并发刷新。
- 不在日志、命令结果或踢出消息输出访问令牌、刷新令牌、客户端密钥、设备代码。
- 如持久保存令牌，建议加密保存，密钥与数据库分离；具体配置与迁移方式待定。
- 网络请求和数据库操作不阻塞游戏主线程，设置超时、有限重试及限流。

### 4.6 调研限制

官方手册声明部分 API 仍处于试验阶段。当前只核对手册，没有用真实账号验证接口返回、权限申请、UUID 一致性及授权撤销行为。

手册链接的 OpenAPI YAML 本次未成功读取，不能声称已核对 OpenAPI 契约。实现前应读取 OpenAPI 并联调；手册说明与 OpenAPI 不一致时以 OpenAPI 为准。

## 5. 建议的核心流程（以 Q1 选 A 为前提）

1. 可信登录流程提供当前游戏 UUID。
2. 查询其名额、禁用状态与归属验证有效期。
3. 已获准且验证有效的角色按规则入服。
4. 未绑定或需要重新授权的角色进入绑定流程。
5. OAuth 完成后获取 LittleSkin UID 和全部角色 UUID。
6. 当前游戏 UUID 不在列表中时拒绝绑定，并说明登录角色与授权账号不匹配。
7. 在数据库事务中检查禁用状态、账号名额和已有记录；同一 UUID 重复请求保持幂等。
8. 名额可用时添加当前角色；已满则提示账号已用名额及申请管理员处理的方式。
9. 在有效的游戏会话中解除限制，或提示玩家重新连接；具体体验由绑定模式决定。

获取全部角色列表只用于归属核验，不自动把全部角色占入名额。是否允许一次授权后自动识别同账号其他角色，需另行决定；必须保持验证新鲜度及原子分配。

## 6. 下一轮必须细化的业务规则

| 主题 | 待确认问题 | 建议起点 |
| --- | --- | --- |
| 默认上限 | 默认 N 是多少？是否允许账号独立上限？0 表示禁用还是无限？ | 全局默认 + 账号覆盖；无限值应明确约定 |
| 强制绑定 | 在入服前拒绝并给链接，还是进入后限时绑定？ | 两种模式可配置，但首期主流程需要选定 |
| 受限入服 | 未绑定时是否禁止移动、交互、指令、聊天、拾取和伤害？多久超时？ | 如选择入服后绑定，应给明确指令白名单和超时行为 |
| 可选绑定 | 不强制时游客能否正常游玩？ | 明示这种模式不能完整限制未知账号的小号 |
| 名额释放 | 仅管理员，还是玩家也能释放自己的名额？是否冷却？ | 初期管理员处理，避免反复换号规避限制 |
| 删除含义 | 删除记录后能否重新绑定？ | 释放名额、解除授权、禁止角色分别定义 |
| 上限调低 | 已有 3 个名额改为 2 时保留谁？是否踢在线玩家？ | 不任意删记录；明确超额处理方案 |
| 角色变化 | 改名、删除重建、转移账号后如何处理？ | 按 UUID 与新归属复核，不按同名恢复权限 |
| 归属复核 | 每次登录、定时、缓存到期还是只绑定时检查？ | 缓存 + 到期复核；周期待定 |
| 授权撤销 | 是否立即拒绝、给予宽限或要求重新绑定？ | 区分明确失效与临时网络故障 |
| 外部账号状态 | 是否因 LittleSkin 封禁状态拒绝入服？ | 单独配置，不能默认当成本服封禁 |
| 故障 | LittleSkin 不可用、数据库不可用分别怎么办？ | 新绑定拒绝；已验证角色可配置有限缓存宽限 |
| 例外 | 管理员、OP、特定 UUID 是否绕过？ | 显式权限或例外记录，并说明是否占名额 |
| 服务器范围 | 单服独立，还是群组服共享 N？ | 单服默认；共享使用显式 scope 标识 |
| 在线变更 | 管理员禁用或释放在线角色后立即踢出吗？ | 禁用立即生效；其他动作单独规定 |
| 数据保留 | 历史角色、审计、失效授权保留多久？ | 设清理策略，但清理不能意外重置限制 |

## 7. 命令与权限草案

以下只是待审阅的命令设计，不是已实现接口。

| 命令 | 预期用途 |
| --- | --- |
| `/littleslot bind` | 发起自己的绑定或重新授权 |
| `/littleslot status` | 查看自己绑定、名额与验证状态 |
| `/littleslot account <uid>` | 管理员查看账号上限及角色名单 |
| `/littleslot player <uuid>` | 管理员查看角色归属、名额和禁用状态 |
| `/littleslot release <uuid>` | 释放入服名额；不代表禁止再次申请 |
| `/littleslot limit <uid> <数量>` | 修改账号独立上限 |
| `/littleslot limit <uid> reset` | 恢复全局上限 |
| `/littleslot block <uuid>` | 禁止角色入服 / 再申请，是否同时释放名额待定 |
| `/littleslot unblock <uuid>` | 解除禁止，不自动授予名额 |
| `/littleslot reload` | 校验并重载允许热更新的配置 |
| `/littleslot doctor` | 检查配置、数据库、OAuth 设置与登录环境 |

建议控制台可执行所有管理命令；每类管理操作独立权限节点。用户名可用于搜索，但最终变更应定位到确定 UUID，重名或历史名歧义不可猜测。

管理员强制修改归属会绕过 OAuth 证明，需要确认是否提供；如提供，应与已验证绑定区分并记录审计。删除、修改均应给出明确结果与影响，不依赖多步菜单完成常规操作。

## 8. 配置草案

建议按以下类别组织，具体键名与默认值在规则确认后再定：

- 名额：默认上限、账号覆盖、作用范围、例外策略。
- 绑定：强制模式、等待时间、提醒间隔、会话有效期、允许命令、重复请求冷却。
- OAuth：流程、客户端 ID、密钥来源、回调地址、监听地址与端口、连接和读取超时。
- 复核：缓存有效期、刷新周期、授权失效处理、外部故障宽限。
- 数据库：SQLite 文件路径；MySQL 地址、库名、用户名、密码来源、TLS、连接池与超时。
- 消息：绑定提示、链接、授权成功 / 失败、归属不匹配、名额已满、禁用、超时、服务不可用、管理结果。
- 展示：中文默认、是否需要英文、颜色格式、链接点击能力、旧版本降级展示。
- 运维：日志级别、审计保留、请求限流和诊断信息。

消息占位符建议包含角色 UUID、显示名、已用数、上限、授权链接、剩余秒数；展示账号 UID 等信息的范围需要限定。配置重载先验证再替换，失败保留旧配置。数据库切换与监听端口变化是否要求重启，应明确提示。

## 9. 数据模型与一致性草案

逻辑实体可包含：

- 账号：UID、独立上限、管理状态、创建 / 更新时间。
- 角色：UUID、当前已验证 UID、最后已知名字、归属验证时间。
- 名额：scope、UUID、所属 UID、分配时间与来源。
- 授权：UID、加密令牌、过期时间、版本 / 刷新协调信息。
- 禁止记录：目标类型、目标标识、原因、操作者与时间。
- 审计：动作、目标、变更前后、操作者、时间。
- 数据库版本：支持后续结构升级。

待 Q1、跨服范围和禁用语义确认后再确定表结构。绑定会话可优先放内存，重启失效；如需跨实例回调则需共享或路由设计。

必须保证：同一 scope 内角色名额唯一；检查剩余名额与写入在同一原子操作中完成；失败不会留下半条绑定；跨服不能只依赖 JVM 内锁。

SQLite 和 MySQL 通过存储层封装差异，分别验证事务行为。切换数据库连接不等于迁移数据。是否首期提供迁移命令待确认；迁移应包含停写或维护窗口、备份、计数与关键约束校验、失败恢复路径。

## 10. 版本与部署范围

文档已核对：Paper 26.1+ 使用 Java 25；`plugin.yml` 的 api-version 高于服务端时会拒绝加载。因此以 26.2 验收，不代表应将最低加载版本也设为 26.2。

建议使用稳定 Bukkit API 和传统 plugin.yml，避免 NMS、CraftBukkit 内部类及不必要的 Paper 专属依赖。插件字节码、第三方库最低 Java 要求和服务器运行 Java 分开规划。

仍需用户明确：

1. 最低必须支持 1.8、1.12、1.16，还是只尽量兼容？
2. 是否必须同时验收 Spigot 与 Paper？
3. 是否有 Velocity / BungeeCord、具体身份转发设置？
4. 是否包括 Folia、Geyser/Floodgate、混合登录插件？这些不自动包含在普通 Bukkit/Paper 兼容承诺中。
5. 单服预计在线量、账号量和绑定高峰规模？

应以“已测试版本”和“预计兼容版本”区分发布说明，不承诺未经测试的全版本兼容。

## 11. 验收场景草案

后续获得实现授权后，至少验证：

1. 正常 OAuth 绑定，正确保存 UID 与 UUID；错误账号不能绑定当前角色。
2. N 个角色成功，第 N+1 个被拒绝；同 UUID 重试不重复占用。
3. 两个角色争抢最后一个名额只有一个成功；共享数据库时跨实例同样成立。
4. 改名不依赖用户名重新识别；删除重建、归属转移按已确认规则处理。
5. 授权拒绝、过期、重复回调、state 错误和轮询限流处理正确。
6. 令牌刷新不丢失新令牌；权限不足的 HTTP 200 不被当作成功。
7. 接口超时与数据库故障不阻塞主线程、不意外放行未知角色。
8. release、block、limit 的行为与在线生效策略一致，审计可查询。
9. SQLite / MySQL 数据重启后保持；如提供迁移，迁移前后关键数据一致。
10. 配置错误重载保留原配置；自定义文本及占位符工作正常。
11. 26.2 与选定旧版本实际启动、登录、绑定、踢出和命令可用。

## 12. 官方参考资料

- LittleSkin 手册：https://manual.littlesk.in/
- OAuth 总览：https://manual.littlesk.in/advanced/oauth2/
- 授权代码流：https://manual.littlesk.in/advanced/oauth2/authorization-code-grant
- 设备代码流：https://manual.littlesk.in/advanced/oauth2/device-authorization-grant
- LittleSkin API：https://manual.littlesk.in/advanced/api
- OpenAPI（本次未成功读取）：https://manual.littlesk.in/littleskin-api.openapi3_1.yaml
- Blessing Skin 用户信息：https://blessing.netlify.app/api/user
- Blessing Skin 角色接口：https://blessing.netlify.app/api/players
- 服务端外置登录：https://manual.littlesk.in/yggdrasil/server
- Paper Java 要求：https://docs.papermc.io/paper/getting-started/
- Paper plugin.yml：https://docs.papermc.io/paper/dev/plugin-yml/

## 13. 换设备后的继续方式

可直接发送：

> 请阅读 require.md 和 PLANNING.md，继续细化 LittleSlot 需求。先与我确认第 3 节的三个问题，再分批讨论第 6 节规则与部署范围。区分已确认需求、建议和待实测事项；在我明确同意开始实现前不要编写插件代码。

下一步产出应是确认后的实现规格：名额语义、完整玩家流程、异常行为、命令权限、配置默认值、数据模型、部署说明和测试矩阵。当前不要将本文建议直接视作已批准的产品决策。
