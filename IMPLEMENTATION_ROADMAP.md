# LittleSlot 实现规格与编码路线

整理日期：2026-09-15（登录方向更新）
当前阶段：已获用户编码授权，开发进行中。  
本文是集中规格与路线，实际完成范围见第 20 节。2026-09-15 新登录方向已确认，初版已同步到代码。

## 1. 阅读规则与工作边界

本文将 `require.md`、`PLANNING.md` 及本次讨论中的最新回答合并为一套规格，避免继续按历史备选方案开发。

- **已确认**：用户明确提出或通过交互表选择的要求，是后续实现必须遵守的范围。
- **实现建议**：为落实已确认要求整理的模块、接口、表结构、状态及测试设计；不表示用户逐项批准了具体技术选型。
- **待定 / 待核验**：不允许默认为已确认，也不能据此宣称已通过测试或获第三方批准。
- 文中的命令名、配置键名、接口路径、表名都是设计草案，尚未存在对应代码。
- 2026-09-14 用户已明确授权开始编码；原先“只写 Markdown”的限制已解除。业务偏好已经确认的部分无需重复询问。

原始需求保留在 [require.md](require.md)，讨论历史保留在 [PLANNING.md](PLANNING.md)。本文与历史方案冲突时，按本文集中整理后的最新决定理解；若用户随后提出新要求，应同步更新本文。

## 2. 项目目标与首期范围

### 2.1 已确认的目标

实现公开发布的 Bukkit / Paper 插件，限制每个 LittleSkin 账号在指定服务器范围内获准入服的角色数量。

- 面向不同服主和服务器环境，不围绕项目作者自己的某个服务器定制。
- 以 LittleSkin **UID** 识别账号，以角色 **UUID** 识别角色。
- 用户名只能用于展示与辅助搜索，不能作为归属和名额主键。
- 限制的是固定入服 UUID 名单，离线后仍占名额；不是同时在线人数，也不是限制皮肤站创建角色的总数。
- 使用环境为 LittleSkin 登录，可选兼容正版；鉴权环境由服主部署保证。开启正版兼容时按 Mojang 用户名查询结果与进服 UUID 比较，匹配则放行，不要求绑定、不占名额。
- PlayerJoinEvent 触发就检查，不等待其他登录插件完成鉴权。检查与绑定期间限制操作，通过后恢复。
- 默认 SQLite，支持 MySQL，并在首期提供双向迁移。
- 管理员能够用一行指令完成查询、释放、修改上限、封禁等常规操作。

### 2.2 已确认的平台目标

| 项目 | 范围 |
| --- | --- |
| 服务端 | Bukkit / Paper 体系 |
| 最低必须验收的 Minecraft 版本 | 1.16.5 |
| 新版本目标 | 原需求指定的 26.2；实际可获取版本和依赖在实施时核对 |
| 更旧版本 | 尽力兼容，不作未经测试的保证 |
| 登录入口 | PlayerJoinEvent；配置选择是否兼容正版；不适配第三方登录插件或身份桥接 |
| 群组服 | 同一 MySQL 数据库、相同 scope 共享名额，默认单服独立 |
| 首期不含 | Folia、基岩版专门适配，以及第三方登录状态、来源桥接、UUID 改写/映射适配 |

平台版本与 Java 版本是不同约束，不能把最低 Minecraft 版本直接当作 Java 编译目标。具体 JDK、字节码级别、构建工具和依赖版本在兼容性核验阶段确定。

### 2.3 本次已排除的旧方案

- 不是每个角色都必须单独 OAuth；已同步的同账号其他角色可免重复授权。
- 不是只记录已进服 UUID；需要同步授权账号的完整角色归属索引。
- 不是命中归属索引就无条件放行；仍要执行名额及禁用规则。
- 三种模式中没有额外要求提供“服主自行部署公共协议后端”这一独立模式。
- 公共后端不接收目标游戏 UUID、用户名、玩家账号或实例身份等额外业务字段。
- 公共后端不管理入服名额、不长期保存玩家授权、不主动刷新角色列表。
- 不按轮询次数计费，不把订单号直接用作 API Key。

## 3. 身份与核心概念

| 概念 | 定义与用途 |
| --- | --- |
| LittleSkin UID | 名额所归属的账号；角色集合改变后仍要保持同一账号身份 |
| 角色列表 UUID | LittleSkin 返回的档案 UUID，直接与进服 UUID 比较，不转换或按同名映射 |
| 进服 UUID | Bukkit Player 提供的 UUID，作为本次检查对象，不追溯其他组件是否改写 |
| scope | 明确选择的名额和归属数据共享范围；不是 API Key |
| 角色归属索引 | 某次授权验证得到的 UID 与全部角色 UUID 的关联 |
| 入服名额 | 某个 UUID 已经获准入服并占用该 UID 的一个名额 |
| 验证有效期 | 归属快照可用于免重复授权的期限，默认 30 天 |
| 登录分类 | 按配置、Mojang 查询与超时自选流程作出的分类，不是第三方插件的鉴权证明 |
| API Key | 公共服务调用凭证及额度归属，不证明玩家的游戏身份 |

### 3.1 身份识别边界（2026-09-15 新确认）

取消旧的第三方登录适配、等待鉴权、未知来源默认放行与原始 UUID 映射方案。

- 不检测或等待 AuthMe、FastLogin、MultiLogin、StellarMultiAuth 等组件登录完成，不自动修改其配置。
- PlayerJoinEvent 只表示服务端已接纳玩家，不保证其他插件鉴权完成；用户明确选择入服即检查并发送要求。
- 不处理 UUID 改写、离线 UUID 或原始 UUID 映射；直接使用进服 UUID。服主负责 LittleSkin（可选正版）鉴权及身份一致性，普通离线账号与其他外置来源不属于承诺支持范围。
- 正版兼容关闭：所有人进入 LittleSkin 归属及名额检查，不请求 Mojang；已有有效归属仍免重复绑定。
- 正版兼容开启：每次入服按当前用户名查询 Mojang，返回 UUID 与进服 UUID 相同按正版放行；不同或官方明确查无此名则进入 LittleSkin 检查。
- Mojang 查询超时：提示玩家选择“正版 / LittleSkin”。选择 LittleSkin 进入 LittleSkin 检查；选择正版则临时放行并入库，后续每次入服仍重新查询，再次超时仍重新选择。
- 自选正版不写成“已验证正版”。用户接受超时期间 LittleSkin 玩家可能借此临时绕过限制。
- 网络请求在异步执行；旧的临时放行记录或历史成功缓存不能替代每次入服的新查询。异步结果必须核对同一次连接。
- 正版兼容开关默认值、超时以外的限流/服务错误处理、玩家未选择时的期限尚未明确，不能写成已确认规则。

## 4. 名额与自动绑定规则

### 4.1 已确认的默认值

| 设置 | 默认与语义 |
| --- | --- |
| 全局账号上限 | 2 个角色 |
| 账号独立上限 | 支持覆盖全局设置 |
| 上限 -1 | 无限 |
| 上限 0 | 禁止新增名额；按已确认的调低上限规则保留旧名额 |
| 玩家自助释放 | 开启，可关闭 |
| 自助释放冷却 | 7 天，可配置 |
| OP 自动绕过 | 不提供默认名额绕过 |
| 默认共享范围 | 单服独立 |

### 4.2 两类记录必须分开

账号 UID=U 有角色 A、B、C，上限 2：

| 动作 | 归属索引 | 已占名额 | 结果 |
| --- | --- | --- | --- |
| A 首次授权 | 同步 A/B/C → U | A | A 放行 |
| B 首次进服 | 命中已有索引 | A、B | B 免授权并放行 |
| C 首次进服 | 命中已有索引 | A、B | 满额拒绝，不能因索引中存在而放行 |
| A 再进服 | 命中已有索引 | A、B | 不重复计数 |
| 创建新角色 D 后进服 | 若尚未同步则未知 | 不变 | 提示 OAuth |

获取完整列表不会让从未进服的角色提前占名额。角色获得名额的检查和写入必须是一个原子操作。

### 4.3 释放规则

- 冷却按 **UID + scope** 计算，不按单个角色分别计算。
- 从一次成功自助释放开始计时，默认 7 天。
- 首次允许立即释放；失败操作不启动冷却。
- 管理员释放不受玩家冷却，也不重置玩家冷却。
- 释放在线角色立即踢出；共享 scope 时需要通知角色所在实例。
- 释放不是封禁，该 UUID 以后仍可以按正常名额规则重新申请。
- 释放名额不应删除账号身份、整个归属索引或冷却历史，否则可能重置限制。
- 实现建议：玩家只能操作经验证归属于自己的账号中的名额；不能因为其知道 UID/UUID 就允许修改。归属过期或无可靠会话时须重新建立权限依据。

### 4.4 上限调低与封禁

- 已有名额超过新上限：保留旧名额及其入服资格，禁止新增；随着释放逐步回到上限。
- 管理员封禁角色：立即踢出，保留名额，拒绝后续申请和自动入服。
- 解除封禁：不自动恢复已经被释放的名额。
- 封禁与释放应是独立命令和审计动作。
- 上述管理规则用于受 LittleSlot 管理的角色；不得未经确认扩大为全服正版封禁系统。

## 5. 完整玩家流程

### 5.1 进入服务器

1. PlayerJoinEvent 触发，记录本次连接、玩家名和进服 UUID，开始检查并限制操作；不等待其他插件鉴权。
2. 正版兼容关闭直接进入步骤 5；开启则按玩家名查询 Mojang。
3. 返回 UUID 与进服 UUID 相同：按正版放行，不访问公共服务、不分配 LittleSkin 名额。
4. 明确不匹配或查无此名：进入步骤 5。查询超时则询问玩家，选择 LittleSkin 进入步骤 5；选择正版则临时放行并入库。本次流程结束，后续每次入服仍查询，超时仍询问。
5. 进入 LittleSkin 检查；明确封禁或其他确定拒绝原因按已有规则拒绝。
6. 查询本 scope 的角色归属索引。未知 UUID 或归属过期：要求授权绑定。
7. 归属已知且有效：原子检查已有名额、上限及禁用状态，不重复要求绑定。
8. 已有名额直接使用；未占名额且有空位则分配；满额拒绝。
9. 更新必要的本地展示和审计信息，成功后解除限制。

### 5.2 授权完成后的本地处理

1. 插件收到 UID 与角色 UUID 列表，校验格式和完整成功状态。
2. 从本地会话找到发起授权的进服 UUID（不追溯或转换原始 UUID），不从公共请求推导玩家身份。
3. 检查玩家是否仍处于同一有效游戏会话，避免旧回调放行后来连接。
4. 返回列表不包含当前进服 UUID：立即踢出，不授予名额。踢出画面说明授权账号不包含当前角色，提示使用对应 LittleSkin 账号授权；如服务器改写 UUID，请联系服主。公共调用仍按成功取数计费。
5. 当前角色在列表中：同步本 scope 的账号归属快照，更新验证期限，处理旧归属变化。
6. 原子执行名额检查与分配，不能因为授权期间管理员改了上限或封禁而绕过最新规则。
7. 成功解除限制；满额等本地拒绝不撤销已完成的公共调用收费。

实现建议：不匹配的授权结果不应用于给当前玩家建立管理权限，也不直接修改无关账号的本服名额。

### 5.3 默认交互

| 项目 | 已确认默认 |
| --- | --- |
| 强制绑定 | 开启，可配置 |
| 等待期限 | 5 分钟，可配置 |
| 提醒间隔 | 30 秒，可配置 |
| 限制 | 移动、交互、聊天及非白名单命令 |
| 白名单 | 绑定、状态、释放、帮助等必要命令 |
| 超时 | 踢出 |
| 默认语言 | 中文 |

实现建议：覆盖放置/破坏、背包和容器、拾取/丢弃、伤害、载具、传送等可能绕过限制的事件，并与登录插件自身的限制协调。具体 API 事件覆盖以版本实测确定。

非强制模式需要清楚说明游客可以游玩，无法保证对未知账号的完整名额限制。消息、链接、错误原因、剩余时间与命令白名单均应可配置。

## 6. 归属有效期、同步与变化

- 默认有效期 30 天，可配置；三种 OAuth 模式一致。
- 同一账号在本 scope 下一次有角色进服时，如快照已过期，则重新授权。
- 成功后更新完整角色列表和该账号的有效期；普通登录、查询命中不能不断延长验证期限。
- 到期不自动释放名额，重新验证不重复占名额。
- 不长期保留刷新令牌，不执行后台自动刷新或公共定时复核。
- 新建角色未出现在旧快照时，按未知 UUID 提示绑定。
- 重新验证发现旧 UUID 已不属于账号：撤销旧归属和旧名额，在线角色立即踢出。
- 原角色改名仍按 UUID 识别，不重新占名额。
- 删除重建不能凭同名继承旧角色资格。
- 新归属必须有新的验证依据，不能自动把旧名额迁给另一账号。
- 无关 scope 的授权和角色名单不自动共享；同一 API Key 不改变这个边界。

实现建议：账号同步记录版本或验证时间，避免并发返回的较旧快照覆盖较新快照。新归属与旧归属冲突时在事务中处理，明确记审计，不能留下同 scope 同 UUID 的双重有效归属。

## 7. 故障、欠费与临时放行

### 7.1 已确认行为

- 后端无法完成验证时，默认临时放行，策略可配置。
- 这包括此前问题表讨论的服务不可用、Key 失效或额度不足等无法取得新验证结果的情况。
- 有效本地记录继续按本地规则使用，不要求每次进服访问公共服务。
- 临时放行不写入虚假的成功归属，不自动授予永久名额，也不刷新验证有效期。
- 角色不匹配、封禁、名额已满等**明确拒绝**不能包装成“服务无法验证”而放行。
- 服务恢复后，分批检查临时放行的在线玩家；需要授权者重新进入受限状态并获得 5 分钟绑定期。
- 恢复检查本身不自动发起新的付费授权任务。

### 7.2 实现建议与需要明确区分的边界

- 用不同原因码区分网络超时、上游错误、Key 无效、欠费、用户拒绝、业务不匹配、数据库不可用。
- 用户主动取消授权应按绑定流程处理，不能作为外部故障自动放行的入口。
- 故障探测使用不计费、有限频率的可用性检查或明确操作，不用重复创建 OAuth 会话探测。
- 数据库不可用与 OAuth 不可用是两类问题。数据库故障下无法原子分配名额，具体默认策略需在实施规格中显式落实，不把网络故障选项隐式扩展到所有存储错误。
- 不可用状态应有退避、恢复检查和日志节流，避免断网导致主线程阻塞、刷屏或请求风暴。
- Mojang 超时自选正版放行与 OAuth 故障放行分别记录，不能将非正常路径记作验证成功。

### 7.3 Mojang 查询超时自选与持久化（2026-09-15 新确认）

- 此规则独立于 OAuth/公共后端故障策略；Mojang 超时后必须先询问，不能直接套用 OAuth 故障临时放行。
- 选择 LittleSkin：进入有效本地归属、绑定与名额检查；不是每次都强制重新 OAuth。
- 选择正版：临时放行并持久化；不创建 LittleSkin 归属、不占名额、不刷新归属有效期，也不写入“正版验证成功”。
- 用户所说“每次访问查询”按每次入服解释，不是每条命令、每次移动或公共后端轮询。数据库中的临时记录不能跳过下次查询；再次超时仍重新询问，不记住选择后自动放行。
- 后续查询匹配则结束临时状态并按正版放行；明确不匹配或无此名则转入 LittleSkin 检查，不延续旧的正版临时放行。
- 实现建议：按 scope + 进服 UUID 保存临时状态，并记录最近展示名、选择时间、超时原因和最近复查结果；具体字段和保留期在实现时确定。数据库写入失败行为需显式定义，不能声称已入库。
- 未确认在线周期性查询频率，不擅自增加后台无限轮询。第 7.1 节恢复流程针对 OAuth 故障，不替代本节的入服查询和超时自选。

## 8. 三种 OAuth 模式

### 8.1 模式对照

| 模式 | 执行 OAuth 的位置 | 配置 | 额外入站网络 |
| --- | --- | --- | --- |
| 公共授权后端 | 项目公共服务 | 公共服务地址、API Key | 面板服不需要，公共端需要 HTTPS |
| 自配授权代码流 | 插件服务端 | 自有 Client ID、Secret、回调地址、监听设置 | 需要可到达插件的 HTTPS 回调 |
| 自配设备代码流 | 插件服务端主动请求 | 自有 Client ID、设备流权限 | 不需要回调入站端口 |

三种模式均要求服务器能访问所需的外部 HTTPS 服务。用户在 LittleSkin 网页完成授权，插件不收取密码。

三种模式都在取得数据后删除 OAuth 令牌，不长期保存刷新令牌。为读取 UID 与角色列表保留完成本次请求所需的短期令牌即可。

### 8.2 已调研的接口，实施前必须复核

| 用途 | 地址 / 权限 |
| --- | --- |
| 用户身份 | `GET https://littleskin.cn/api/user`；`User.Read` |
| 全角色 UUID | `GET https://littleskin.cn/api/yggdrasil/sessionserver/session/minecraft/profile`；`Yggdrasil.PlayerProfiles.Read` |
| 授权代码入口 | `https://littleskin.cn/oauth/authorize` |
| 授权代码兑换 | `https://littleskin.cn/oauth/token` |
| 设备代码申请 | `https://open.littleskin.cn/oauth/device_code` |
| 设备代码轮询 | `https://open.littleskin.cn/oauth/token` |

基础 scope 为 `User.Read Yggdrasil.PlayerProfiles.Read`。本项目不需要 `Yggdrasil.PlayerProfiles.Select`、修改角色权限或创建 Minecraft 登录令牌权限；Read 与 Select 的冲突需要在实现中避免。

历史文档核对结果：

- API 请求带 Bearer 令牌和 `Accept: application/json`。
- 某些错误可能在 HTTP 200 的 JSON 中表示，必须判定业务响应，不能只看状态码。
- 访问令牌当作不透明值，不自行解析 UID。
- 授权代码流的 state 回传等安全行为必须联调确认，不能因为文档示例未列出就省略会话校验。
- 当时手册说明授权代码流不支持 PKCE；实施时重新核对，不当作永久事实。
- 设备代码流需要应用白名单；测试模式通常仅创建者可授权，生产开放另需申请。
- 设备流按响应 interval 轮询，正确处理 pending、slow_down、拒绝和过期；不把用户代码与服务端设备代码混用。
- 因不需要长期刷新，本方案不为持久授权额外申请 `offline_access`。

这些是调研记录，不是当前已完成真实账号联调的声明。OpenAPI 契约此前未成功核对，列入第 18 节前置核验。

## 9. 公共后端的最小职责与协议

### 9.1 已确认的职责

公共服务只完成插件提出的授权取数申请，成功计费，返回最小结果。

它不接收目标角色身份，不判断玩家应否入服，不保存本服名额，不为玩家提供账户管理，不提供后台自动同步服务。

必要的 API Key 校验、额度、计费去重、短期会话和发卡接入属于支撑功能，不借此扩展成综合管理平台。爱发电接入可在代码中分模块，是否独立进程部署是技术选型，不改变业务范围。

### 9.2 请求与响应草案

| 操作 | 输入 | 输出 |
| --- | --- | --- |
| 创建授权申请 | API Key、随机请求编号 | 验证地址、查询地址、必要的期限/轮询提示 |
| 查询申请 | API Key、查询地址标识 | 等待/成功/拒绝/失败/过期状态 |
| 成功结果 | 无额外玩家输入 | LittleSkin UID、角色 UUID 列表 |

目标 UUID、玩家名、邮箱、游戏会话、服务器显示名不上传。请求编号只是幂等关联，不编码玩家身份。

成功载荷示意（仅文档，不是既有 API）：

```json
{
  "status": "success",
  "result": {
    "uid": 12345,
    "profiles": ["11111111-1111-4111-8111-111111111111"]
  }
}
```

只返回 UUID，不返回角色名称、邮箱、头像、皮肤属性等无关内容。UID 是维持同账号长期名额身份的必要字段，不能以可变化的角色列表替代。

### 9.3 调用顺序

1. 插件在本地生成请求编号，关联当前游戏会话；带 Key 创建申请。
2. 后端校验 Key、请求新鲜度、额度和限流，创建一次性 OAuth 会话。
3. 后端返回验证地址和查询地址；插件只把验证地址发给玩家。
4. 玩家在 LittleSkin 授权，浏览器回到公共后端。
5. 后端校验 state 和会话状态，兑换令牌，读取 UID 和完整 UUID 列表。
6. 后端原子保存短期结果及成功扣费状态，随后向查询方提供成功结果；读取结束不长期保留令牌。
7. 插件轮询领取结果，执行本地归属匹配和名额事务。
8. 同一申请重复查询、重复回调、结果重领不重复收费。

### 9.4 鉴权与隔离

- Key 放鉴权头或等效私密请求位置，不出现在玩家链接和日志中。
- 查询地址对应的会话须绑定创建它的 Key 权限，知道地址不代表有权领取。
- 一个 Key 可以多服使用；共用 Key 就是共用信任边界，不承诺同 Key 下不同服互相保密。
- 需要隔离的服应使用不同 Key；不能暗中再强制上传实例标识。
- API Key 与会话标识都不构成游戏身份凭证；本地按第 3、5 节分类和核对进服 UUID，鉴权环境由服主保证。
- 实现建议：仅请求受信任的服务地址；携带 Key 访问返回的查询 URL 前校验目标，防止把凭证转发到非预期域名。

## 10. 计费、额度与发卡

### 10.1 已确认的计费语义

| 情况 | 计费 |
| --- | --- |
| 成功取得合法账号角色结果 | 一次 |
| 插件发现当前角色不匹配 | 仍计费 |
| 插件发现名额已满或当前角色被禁止 | 仍计费，公共端取数已成功 |
| 玩家取消、授权超时、上游接口失败 | 不计费 |
| 重复回调、轮询、相同结果重领 | 不另计费 |
| 有效本地索引直接入服 | 不调用、不计费 |
| 过期后用户明确发起新的授权并成功 | 新的一次 |

成功必须是可以构成完整 UID + UUID 列表结果的业务成功，不能在 UID 未读取成功、列表格式损坏或权限错误时扣成功费用。合法空列表与接口失败必须区分。

### 10.2 额度一致性建议

- 每 Key 独立额度，支持多服共用、撤销与更换。
- 请求创建可预占一次额度，成功时结算；失败或超时释放预占。
- 同时发起多个请求不能透支，不能仅凭进程内锁保证一致性。
- 成功结果持久化与扣费落账应有同一原子提交边界或等价恢复设计。
- 不能已经扣费却无任何可领取结果；也不能免费返回成功结果后再尝试不可靠扣款。
- 领取响应丢包不回滚已成功取数与计费，10 分钟内允许重复领取。
- 失败不收费不等于无限请求，创建、等待查询和回调均应有独立限流。
- 更换 Key 的技术设计应保留原额度与请求归属，不因为更换凭证重复扣费；具体最小运维入口在实现阶段确定。

### 10.3 结果过期与去重

- 成功结果默认保留 10 分钟。
- 过期后相同请求编号只返回“结果已过期”，不重新调用上游，不重新扣费。
- 用户明确重开授权时使用新请求编号，成功算新一次。
- 最小计费与去重记录默认保留 180 天，期限可配置。
- 超过去重窗口的旧请求必须拒绝，不能因数据库已经清理而当作新请求。
- 实现建议：选择带创建时间的随机编号或等价可验证请求格式，并规定时钟误差与接受窗口；不能只使用清理后无法判龄的随机字符串。
- 订单防重复兑付与请求去重是不同保留策略，不能直接把180天套到所有订单并允许旧订单重新充值。

### 10.4 爱发电与领取凭证

已确认采用订单号 + 预生成领取凭证：

1. 领取方先生成随机领取凭证及用于订单备注关联的识别码。
2. 购买时将识别码填入备注。
3. 提交订单号与领取凭证。
4. 后端通过爱发电服务端 API 查单，核对订单状态、商品/方案、实付信息及备注关联。
5. 检查订单未兑付，在事务中发 Key 或增加额度并记录兑付。
6. 同一订单重复请求、重复通知不能重复入账。

实现建议：备注识别码与私密领取凭证分开，避免仅看见订单备注就能冒领；Webhook 只作为通知触发，不能只信任外部提交的订单 JSON。平台 API Token 只放服务端。

爱发电的备注能力、查单权限、通知和商品规则需要实测确认；不能把此处设计说成已经可直接对接成功。

**仍待用户确定：具体售价、次数兑换比例、额度是否过期、试用额度及退款规则。** 目前不设置默认商业承诺，不将按调用付费改为未经确认的月订阅。

## 11. 数据保存边界

### 11.1 插件侧逻辑数据模型（实现建议）

| 实体 | 必要内容 | 约束 |
| --- | --- | --- |
| 账号策略 | scope、UID、独立上限 | 相同scope内账号策略唯一；具体覆盖键最终定稿 |
| 归属快照 | scope、UID、验证时间、有效期、版本 | 登录不能刷新验证时间 |
| 角色索引 | scope、角色列表UUID、UID、最后已知展示名（可选） | 同scope同UUID唯一有效归属，与进服UUID直接匹配 |
| 名额 | scope、UUID、UID、分配时间 | 同scope同UUID唯一名额 |
| 自助冷却 | scope、UID、最近成功释放时间 | 不随名额删除而被重置 |
| 角色禁止记录 | scope、UUID、原因、操作者、时间 | 与名额独立 |
| 审计 | 动作、对象、前后变化、操作者、时间 | 不记录敏感凭证 |
| Mojang超时临时放行 | scope、进服UUID、临时状态；建议附选择时间和最近复查结果 | 必须入库；不等于正版认证或名额，每次入服仍查询 |
| 数据库版本 | schema版本和迁移状态 | 可识别不兼容版本，避免破坏性自动操作 |

游戏会话、请求编号到玩家的关联、受限状态及 OAuth 故障临时会话可使用内存；Mojang 超时自选正版的临时放行记录必须入库；重启恢复策略属于实现设计。公共结果已有收费时，避免插件重启后无提示地生成新付费请求，应给出明确恢复/重发结果。

### 11.2 公共服务数据模型（实现建议）

| 数据 | 保存规则 |
| --- | --- |
| Key 摘要、状态、额度 | 持久保存，Key原文不写日志 |
| 订单与兑付依据 | 保存必要的防重复入账和核对字段，期限待定 |
| 扣费和幂等记录 | 默认180天，不保存完整玩家授权结果作为账单 |
| OAuth会话 | 短期保存state校验值、所属Key、状态、期限等，不含插件上传的玩家身份 |
| 成功结果 | UID＋UUID列表，默认10分钟，查询受Key保护 |
| 访问/刷新令牌 | 仅本次取数期间使用，完成后删除，不长期托管 |

后端无法避免看到浏览器回调和 LittleSkin 返回数据；“最小化”指不额外上传、不额外返回、不长期保存无关数据，不宣称后端完全接触不到账号信息。

删除令牌表示不再持久保留并及时解除可用引用，不等同于已向 LittleSkin 执行撤销授权，也不承诺托管语言内存立即物理擦除。不得将原始请求/响应、令牌或完整成功载荷写进长期日志和异常追踪。

## 12. SQLite、MySQL 与群组一致性

### 12.1 已确认

- 默认 SQLite，可通过配置使用 MySQL。
- 共享群组使用同一 MySQL 与相同 scope；不同 scope 默认隔离。
- 同 Key 不自动共享 scope；同数据库不同 scope 不共用名额。
- 首期支持 SQLite → MySQL 和 MySQL → SQLite 双向迁移。

### 12.2 实现建议

- 存储接口封装SQL方言与事务差异；参数化查询，避免拼接玩家输入。
- 名额检查、写入、释放冷却与审计使用事务维持一致性。
- MySQL 使用可靠的行锁或等价机制；SQLite 使用适合其写入模型的事务与忙等待/有限重试。
- 不能只靠单JVM锁防止跨实例超额或重复释放。
- 共享scope的踢出、封禁、归属变更要有跨实例传播；具体采用数据库事件队列或代理通道等方案在技术设计时确定，不默认增设Redis。
- 数据库操作不阻塞游戏主线程；超时和失败有明确状态。

### 12.3 迁移路线

1. 校验源、目标连接和schema版本；不把连接切换当作迁移。
2. 进入维护窗口，协调全部共享写入实例停写。
3. 创建可恢复备份，校验目标是否为空或满足明确导入条件；默认不覆盖未知目标数据。
4. 复制账号、索引、名额、冷却、禁止、Mojang超时临时放行与必要审计，保留UUID、scope、时间和上限语义。
5. 校验计数、引用、唯一约束和关键业务样本。
6. 成功后切换配置并恢复写入；失败保留源数据与恢复路径。
7. 输出迁移结果和明确的后续操作，不把部分复制标为成功。

迁移是否要求重启、MySQL最低版本、字符集/时区和目标冲突处理属于实现阶段必须补齐的技术细则。

## 13. 命令、权限与配置路线

### 13.1 命令草案

| 命令 | 用途 |
| --- | --- |
| `/littleslot bind` | 发起当前角色授权或过期重新授权 |
| `/littleslot status` | 自己的归属、名额、有效期、冷却 |
| `/littleslot release <uuid>` | 玩家释放有权管理的角色，管理员按权限执行管理释放 |
| `/littleslot account <uid>` | 管理员查询账号及名额 |
| `/littleslot player <uuid>` | 管理员查询角色 |
| `/littleslot limit <uid> <数量或reset>` | 修改账号上限或恢复全局 |
| `/littleslot block <uuid> [原因]` | 禁止角色，保留名额并踢出 |
| `/littleslot unblock <uuid>` | 解除禁止 |
| `/littleslot reload` | 校验并重载允许热更新的配置 |
| `/littleslot doctor` | 诊断来源识别、OAuth、数据库和部署问题 |
| `/littleslot migrate ...` | 执行双向迁移，参数在技术规格中定稿 |
| `/littleslot help` | 帮助 |

支持控制台管理，一行完成常规查删改。玩家与管理员释放路径需要明确权限判定。用户名查找如有歧义不得猜测目标。是否提供管理员强改UID归属仍未确认，不擅自加入绕过OAuth的命令。

`doctor`、具体命令拼写、独立权限节点及导出入口属于实现建议；不得把诊断变成默认创建付费授权的行为。

### 13.2 配置分组草案

| 分组 | 内容 |
| --- | --- |
| scope与名额 | scope标识、全局上限、账号覆盖 |
| 进服分类 | 是否兼容正版、Mojang请求超时、自选提示及当前连接状态；不含第三方适配 |
| 绑定 | 强制开关、5分钟超时、30秒提醒、受限事件、命令白名单 |
| 归属 | 30天有效期、过期交互 |
| 释放 | 是否可自助、7天冷却 |
| 故障 | 临时放行、恢复检查、退避和日志节流 |
| OAuth | 三种模式及各自所需凭证、回调/轮询/网络参数 |
| 数据库 | SQLite文件；MySQL连接、凭证、TLS、池和超时 |
| 消息 | 中文默认，全部业务提示、点击链接、占位符 |
| 后端 | Key、额度、限流、结果TTL、去重TTL、支付对接参数 |

实现建议：重载先完整校验，失败保留旧配置；数据库、scope、OAuth模式和监听端口等结构性变化提示重启或迁移要求。未知Key、拼写错误和不合法上限不得静默忽略。

## 14. 进服分类路线（替代第三方登录适配）

1. 统一在 PlayerJoinEvent 启动流程，退出时清理会话；异步结果不能影响后来重连的玩家。
2. 增加正版兼容开关；关闭时所有人进入 LittleSkin 检查，开启时每次入服查询 Mojang 用户名对应 UUID。
3. 匹配放行；明确不匹配/无此名进入 LittleSkin 检查；超时显示“正版 / LittleSkin”选择。
4. 自选正版临时放行并入库，每次再入服仍查询；自选 LittleSkin 沿用归属索引及名额流程。
5. 授权角色列表不含进服 UUID 立即踢出，踢出画面说明账号/角色不匹配及 UUID 改写时联系服主。
6. 配置、选择提示、绑定要求和踢出消息均可配置，并保留详细中英文说明与注释。

不再开发 AuthMe/FastLogin/MultiLogin/StellarMultiAuth 等登录适配器，不读取其登录状态，不自动开启 premiumUuid，不做登录来源代理桥接或 UUID 映射。已有旧适配代码属于待清理实现，不代表仍是新需求。

共享 MySQL、相同 scope 的名额一致性和跨服踢出仍保留；取消身份桥接不取消这些业务能力。插件公开发布时说明适用环境由服主保证，不宣称支持任意身份映射方案。

## 15. 模块划分建议

| 模块 | 职责 |
| --- | --- |
| 领域核心 | 名额规则、冷却、上限、授权结果应用、业务错误分类 |
| Bukkit入口 | 生命周期、事件、命令、受限状态、玩家提示 |
| 进服分类 | 正版兼容开关、Mojang查询、超时自选与临时放行入库 |
| 跨实例动作 | 共享scope的名额、封禁、释放与踢出传播，不传递第三方登录身份 |
| 插件存储 | SQLite/MySQL、事务、版本与迁移 |
| OAuth提供器 | 公共、自配授权代码、自配设备代码统一结果接口 |
| 公共验证服务 | 短期会话、OAuth取数、结果轮询 |
| 计费支撑 | Key、额度、预占/结算、幂等 |
| 订单兑换支撑 | 爱发电查单、领取凭证、单次入账 |

不预先绑定特定Web框架、数据库框架或大型基础设施。稳定Bukkit API优先，避免NMS和不必要的Paper专有依赖。公共后端与插件可以采用不同运行环境，但API契约必须一致。

## 16. 验收矩阵

### 16.1 身份与名额

- 关闭正版兼容不请求Mojang，所有玩家进入LittleSkin检查；有效归属免重复绑定。
- 开启正版兼容时每次入服查询Mojang；UUID匹配放行，不进入OAuth或名额流程；明确不匹配/无此名进入LittleSkin检查。
- Mojang超时提示两种选择；自选LittleSkin正常检查，自选正版临时放行并入库，绝不标记为正版认证成功。
- 重启后临时记录仍存在，但不能跳过下一次入服查询；再次超时仍询问，明确不匹配转LittleSkin流程。
- PlayerJoinEvent即启动，不等待其他插件登录；退出/重连后旧查询或选择请求不能作用于新会话。
- 返回角色列表不包含进服UUID时立即踢出，踢出画面含可配置的账号匹配说明；不自动追溯UUID映射。
- A授权后B免授权、C因满额拒绝的完整示例通过。
- 同UUID重复进服、重发回调和同账号并发不重复占名额。
- 两个实例争抢最后一个名额只有一个成功。
- 不同scope独立，相同scope共享；同Key不意外共享归属。
- 上限为0/-1、账号覆盖、降低上限保留旧名额按规格执行。

### 16.2 释放、封禁、归属

- 第一次自助释放可执行，第二次受UID+scope冷却限制。
- 管理员释放不受冷却，不重置玩家计时；失败操作不触发冷却。
- 在线释放和封禁立即踢出，跨实例也生效。
- 封禁保留名额，归属移出则撤销旧名额。
- 过期重新授权更新整账号快照但不重复分配旧名额。
- 改名不影响UUID身份，删除重建不凭同名恢复。
- 旧授权结果不会错误解除新连接限制或覆盖较新归属。

### 16.3 OAuth与最小协议

- 三种模式都返回一致的UID+UUID结果。
- 抓取测试请求确认公共创建接口不上传目标UUID、用户名、邮箱、实例字段。
- 正常、拒绝、超时、错误state、重复回调、权限不足HTTP200均正确处理。
- 设备流按interval轮询并处理slow_down，未白名单有明确提示。
- 非创建Key无法领取结果；玩家看到的链接不泄露API Key或查询凭证。
- 结果不包含名称、皮肤属性或邮箱；令牌不进入持久日志。
- 验证完成后令牌不长期保存，10分钟TTL按预期清理。

### 16.4 计费与订单

- 一次成功取数只扣一次；目标不匹配和本地满额仍扣一次。
- 上游错误、取消、超时不扣成功费用。
- 余额仅够一次时并发请求不能透支。
- 扣费提交与结果可领取保持一致，注入崩溃和丢包进行恢复验证。
- 10分钟内重领免费，过期重用旧编号不新扣费。
- 180天窗口外旧请求不能被当作新任务。
- 订单通知和领取并发重复只入账一次；备注码不匹配及伪造订单拒绝。
- Key更换、撤销和多服共享不破坏既有账务归属。

### 16.5 故障、部署与迁移

- 网络和DB任务不阻塞游戏主线程。
- 外部不可用按配置临时放行，明确拒绝不被错误放行。
- 恢复后分批限制并给5分钟绑定期，不自动制造付费任务。
- 三种模式分别验证网络与凭证配置，面板服公共/设备模式无需额外入站端口。
- SQLite/MySQL双向迁移保留名额、冷却、scope和禁止状态，失败可恢复。
- 配置错误有明确反馈，重载失败不破坏当前运行配置。
- 在1.16.5和选定新版本实际启动、登录、授权、踢出、命令及代理组合测试。
- 发布说明列出“已测试”和“预计兼容”，不把构建成功当作服务器验收完成。

## 17. 未定事项与外部依赖

### 17.1 用户明确保留待定的商业参数

- 每个商品的售价和对应调用次数。
- 额度是否过期及充值叠加规则。
- 是否提供试用额度。
- 退款、撤单、已消费额度及发卡失败的处理。

先保留可配置接口和必要状态设计，不能擅自发布价格、收费条款或开启收款。

### 17.2 必须实测或核实，不能视作用户已保证的条件

- LittleSkin 对公共商业应用用途、权限和调用规模的允许范围。
- 官方OAuth和OpenAPI实际契约，尤其state、错误结构、UUID一致性和权限。
- 各服自配设备流应用白名单及生产授权可用性。
- 爱发电领取码备注方案、查单与通知接口、商品销售规则。
- Mojang用户名查询的真实成功/查无此名/超时契约，以及单服和代理子服进服事件、UUID透传的实测；不再要求第三方登录插件API适配。
- 26.2及各Java/API/依赖的实际可用版本与兼容性。
- 公共端真实容量、线路和可用性；此前2核4GB等为估算，不是性能承诺。
- 若正式收费运营，具体主体、隐私/服务条款及适用许可需核实，香港部署不自动消除经营义务。

### 17.3 技术设计阶段补齐的细节

这些不要求重新进行整轮偏好问卷；在既定规则内给出具体设计，遇到业务冲突再提问：

- 数据库故障策略与迁移目标冲突策略。
- 进服分类状态机、跨服动作传播及在线权限校验；无第三方身份桥接。
- 正版兼容开关默认值、Mojang限流/非超时错误、未选择期限与临时记录保留期、落库失败策略。
- OAuth请求时限、结果10分钟计时起点、重启恢复路径。
- 180天去重实现与订单防重复兑付长期记录边界。
- 最小Key管理/轮换/充值入口，是否独立部署支付组件。
- schema、命令权限、配置键、日志与本地审计保留期。
- 并发授权结果排序、scope之间的隔离和账号覆盖上限键范围。

## 18. 获得授权后的编码路线

### 阶段0：契约与可行性核验

读取最新官方契约，选定服务器/Java测试版本，核对Mojang查询契约，确认最小API请求格式，验证OAuth state、UID和UUID响应。列明无法实测的外部条件。商业上线依赖单独跟踪，不为此虚构已经获批。

产出：技术选型、接口契约、适配矩阵、真实联调结果或明确阻塞项。

### 阶段1：领域核心与存储

先落实归属索引和名额分离、上限、冷却、禁止状态、scope、SQLite/MySQL事务与schema升级。用有意义的并发和故障测试验证规则。

产出：可独立验证的核心及存储能力，不依赖真实OAuth才能测试名额规则。

### 阶段2：Bukkit玩家流程与配置驱动的进服分类

实现进服状态、受限行为、消息、命令及配置校验，连接核心。以PlayerJoinEvent为入口，落实正版兼容开关、每次入服Mojang查询、超时自选正版临时放行入库和再次入服复查；角色列表不匹配立即踢出。清理旧登录适配器及其过时配置、测试，不新增第三方鉴权或UUID映射适配。

产出：真实测试服务器上可验证的分类、临时放行、绑定与名额流程；配置及消息提供详细中英文说明。

### 阶段3：三种OAuth提供器

统一提供器结果为UID+UUID列表，实现自配授权代码流、设备流和公共客户端。所有游戏会话关联留在插件本地，公共请求维持最小化。

产出：三种模式的成功、失败、重复请求与超时测试。

### 阶段4：公共验证服务与计费

实现最小申请/查询/回调、API Key、额度、幂等和短期结果。先验证计费与结果原子性及TTL，再接订单兑换。

产出：不含额外业务平台的公共验证服务，可注入故障验证账务不重扣。

### 阶段5：爱发电领取与收费配置入口

实现预生成凭证、查单、幂等发卡/充值。价格和退款规则未确认时保持相关业务配置待定，不代用户上架商品或收款。

产出：可在受控订单条件下验证的兑换链路与缺项说明。

### 阶段6：群组一致性、迁移和故障恢复

完成共享scope并发、跨实例踢出、双向迁移、临时放行恢复、Key轮换与进程重启恢复测试。

产出：多实例及迁移验证报告和服主操作说明。

### 阶段7：整体验收与发布准备

执行第16节矩阵，生成插件与公共端部署文档、配置参考、支持版本清单、迁移与故障说明。根据实际压测决定容量，不用早期估算替代结果。

产出：可审阅的发布候选物和未完成事项。发布、公共部署、商业启用需以当时用户授权为准，不因编码授权自动推定已获授权。

## 19. 官方资料入口

以下链接是前期调研与未来实施核验入口，不表示本文整理当天重新验证了全部外部内容：

- [LittleSkin OAuth总览](https://manual.littlesk.in/advanced/oauth2/)
- [授权代码流](https://manual.littlesk.in/advanced/oauth2/authorization-code-grant)
- [设备代码流](https://manual.littlesk.in/advanced/oauth2/device-authorization-grant)
- [LittleSkin API](https://manual.littlesk.in/advanced/api)
- [LittleSkin OpenAPI](https://manual.littlesk.in/littleskin-api.openapi3_1.yaml)
- [LittleSkin外置登录部署](https://manual.littlesk.in/yggdrasil/server)
- [LittleSkin服务条款](https://manual.littlesk.in/policies/tos)
- [AuthMe](https://github.com/AuthMe/AuthMeReloaded)
- [FastLogin](https://github.com/TuxCoding/FastLogin)
- [MultiLogin](https://github.com/CaaMoe/MultiLogin)
- [authlib-injector](https://github.com/yushijinhun/authlib-injector)
- [爱发电开发者文档](https://guide.afdian.com/creator/developer)
- [Paper运行环境](https://docs.papermc.io/paper/getting-started/)
- [Paper plugin.yml说明](https://docs.papermc.io/paper/dev/plugin-yml/)

## 20. 下一位开发者的开始位置

先阅读本文，保留第17节的待定和待核验状态。确认已有明确编码授权后，从阶段0开始，不重新采用历史公共端上传目标UUID、长期刷新令牌、按轮询收费、每角色单独授权等已被替换的方案。

开发已开始：领域核心、SQLite/MySQL JDBC 存储、Bukkit 入口、三种 OAuth 模式的初版、最小公共服务、并发额度预占、临时放行恢复、管理员查询、离线迁移工具及审计轮询式跨实例动作传播代码已落地。插件主配置、玩家/管理员消息、自配代码流浏览器文字均已提供逐项中英文注释和可编辑默认文件；公共后端浏览器文字另有可覆盖属性文件。2026-09-15 新方向已替换旧第三方登录来源探测：PlayerJoinEvent 直接检查，正版兼容开关控制每次入服的 Mojang 查询；超时自选正版先入库再临时放行，超时自选 LittleSkin 进入本地检查；OAuth 列表不含进服 UUID 时踢出。旧适配类和 plugin.yml 的 softdepend 已删除，相关配置/消息已有中英文说明。真实 Mojang 与 OAuth 账号联调、MySQL 实例、跨服传播实测、真实 MySQL 双向迁移、爱发电和服务器版本验收仍未完成；不要把构建与单元测试通过写成这些工作已验收。

### 20.1 本轮新方向的编码状态

已落实第14节核心路径，数据库 schema 从 1 升到 2，新增按 scope + 进服 UUID 保存的正版超时选择记录；离线迁移随新表改成档案版本 2。Mojang 的 200 合法 UUID、404 查无此名、超时、其余错误分流已有本地 HTTP 测试；旧库升级和临时记录迁移已有 SQLite 测试，GitHub CI 的逐测试矩阵同步更新。默认关闭正版兼容；Mojang 非超时错误当前踢出以便稍后重试，选择超时沿用总计 5 分钟受限期限，临时记录在明确查询结果后清理，清理失败不阻断有效查询。上述是当前实现选型，不宣称用户曾逐项指定。真实服务器上仍须联调进服状态、点击/输入体验与跨实例行为；原始 require.md 和 README.md 保持不变。

### 20.2 可选 PlaceholderAPI 接入

用户已授权先加入 PlaceholderAPI。当前以软依赖注册内置扩展，提供当前会话的 `state`、`scope`、`uid`、`used`、`limit`、`remaining` 占位符；不安装 PAPI 时插件仍应正常启动。数字只在已通过 LittleSkin 名额检查的会话中显示，`state` 只是流程阶段，`bypass` 不能解读为正版鉴权证明。详情见 [PAPI.md](PAPI.md)。已加格式化单元测试和 CI 条目；真实 PAPI 服务器上的注册、重载与计分板显示仍待联调。
